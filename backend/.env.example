# Backend environment example - DO NOT commit real secrets to the repo
# Use this for reference or testing locally; for production set secrets in Render/Vercel

# Option A (recommended): paste the full service account JSON string
# Example (single-line JSON, with \n inside the private key):
# FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"<PROJECT_ID>","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"<CLIENT_EMAIL>",...}'

# Option B: set individual Firebase fields (private key must have escaped newlines)
# FIREBASE_PROJECT_ID=<PROJECT_ID>
# FIREBASE_CLIENT_EMAIL=<CLIENT_EMAIL>
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Other common env vars
# SENDGRID_API_KEY=<sendgrid_api_key>
# SENDGRID_FROM_EMAIL=<from_email>
# NEXT_PUBLIC_API_URL=https://your-backend.example.com
# NODE_ENV=production

# Example local dev variables (optional)
# PORT=3001
# DATABASE_URL=... (if you add one later)

# SECURITY: keep secrets in your host's secret manager (Render/Vercel), not in repo
